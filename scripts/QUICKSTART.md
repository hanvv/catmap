# ⚡ 5分钟快速开始

## 🎯 目标

从 MySQL 表结构自动生成完整的 CRUD 功能模块

## ⭐ 推荐使用修复增强版

使用 `generate-fixed.js` - 已修复所有已知问题！

## 📝 步骤

### 1️⃣ 安装依赖 (30秒)

```bash
cd scripts
npm install
```

### 2️⃣ 配置数据库和表名 (1分钟)

打开 `scripts/generate-fixed.js`，修改前几行：

```javascript
const DB_CONFIG = {
  host: 'localhost',           // 数据库地址
  user: 'root',               // 用户名
  password: 'your_password',  // 📝 改成你的密码
  database: 'your_database',  // 📝 改成你的数据库名
  port: 3306,
}

// 📝 支持多表批量生成！
const TABLE_NAMES = [
  'products',    // 改成你的表名
  'categories',  // 可以添加多个
]
```

### 3️⃣ 运行生成器 (30秒)

```bash
node generate-fixed.js
```

### 4️⃣ 查看结果 (1分钟)

生成的文件在：
- `src/features/{table_name}/` - 功能模块
- `src/routes/_authenticated/{table_name}/` - 路由

### 5️⃣ 添加到侧边栏 (2分钟)

编辑 `src/components/layout/data/sidebar-data.ts`：

```typescript
import { Package } from 'lucide-react' // 选择一个图标

{
  title: 'Products',        // 显示名称
  url: '/products',         // 路由路径
  icon: Package,            // 图标
}
```

### 6️⃣ 启动项目

```bash
npm run dev
```

访问 `http://localhost:5173/products` 查看生成的页面！

## 🎉 完成！

你现在有一个完整的 CRUD 功能模块，包括：

✅ 数据列表页面  
✅ 添加功能  
✅ 编辑功能  
✅ 删除功能  
✅ 批量操作  
✅ 分页、排序、筛选  

## 📖 示例

### 示例 1: 用户管理

```javascript
const TABLE_NAME = 'users'
```

生成结果：`/users` 路由，用户管理页面

### 示例 2: 产品管理

```javascript
const TABLE_NAME = 'products'
```

生成结果：`/products` 路由，产品管理页面

### 示例 3: 订单管理

```javascript
const TABLE_NAME = 'orders'
```

生成结果：`/orders` 路由，订单管理页面

## 🔥 一键生成多个表

创建 `batch-generate.sh`：

```bash
#!/bin/bash

# 要生成的表列表
tables=("users" "products" "orders" "categories")

for table in "${tables[@]}"
do
  echo "生成 $table..."
  # 修改配置并运行
  # sed -i "s/TABLE_NAME = .*/TABLE_NAME = '$table'/" generate-complete-module.js
  # node generate-complete-module.js
done
```

## 📚 更多文档

- [完整指南](./COMPLETE-MODULE-GUIDE.md) - 详细功能说明
- [使用文档](./USAGE.md) - 高级用法
- [README](./README.md) - 项目概述

## ⚠️ 注意事项

1. **备份数据** - 生成前建议提交代码
2. **检查表名** - 确保表名存在
3. **权限检查** - 确保有读取表结构的权限
4. **逐步测试** - 一次生成一个表

## 💡 提示

### 快速测试

使用项目自带的 `users` 表测试：

```javascript
const TABLE_NAME = 'users'  // 改成项目中已有的表
```

### 常见问题

**Q: 连接数据库失败？**  
A: 检查密码和数据库名是否正确

**Q: 表不存在？**  
A: 确认表名拼写正确，区分大小写

**Q: 生成的文件在哪？**  
A: `src/features/{table_name}/` 和 `src/routes/_authenticated/{table_name}/`

## 🎯 下一步

1. 集成真实 API（替换模拟数据）
2. 自定义表单验证
3. 调整表格列显示
4. 添加更多业务逻辑

开始愉快的开发吧！🚀

